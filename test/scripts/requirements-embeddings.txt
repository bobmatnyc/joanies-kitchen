# Python Dependencies for Local Embedding Generation
# Install with: pip install -r scripts/requirements-embeddings.txt

# Core embedding generation
sentence-transformers>=2.2.0,<3.0.0

# Database connectivity
psycopg2-binary>=2.9.0,<3.0.0

# Environment and configuration
python-dotenv>=1.0.0

# Progress tracking
tqdm>=4.65.0

# Numerical operations
numpy>=1.24.0,<2.0.0

# Optional: Torch (required by sentence-transformers, but auto-installed)
# torch>=2.0.0

# Installation Notes:
# - sentence-transformers will auto-download BAAI/bge-small-en-v1.5 (~133MB) on first run
# - psycopg2-binary is used for easier installation (no compilation required)
# - For production, consider psycopg2 instead of psycopg2-binary
